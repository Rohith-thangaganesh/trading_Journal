---
description: Initialize Git and push to GitHub
---

1. Initialize Git repository
// turbo
2. Add all files to staging
// turbo
3. Commit changes
4. Create a new repository on GitHub (https://github.com/new)
5. Copy the repository URL
6. Add the remote origin: `git remote add origin <YOUR_REPO_URL>`
7. Push to main branch: `git push -u origin main`
